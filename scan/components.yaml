components:
  responses:
    TestResponse:
      description: 'Testing Description'
      content:
        application/json:
          schema:
            type: object
            properties:
              errors:
                type: string
